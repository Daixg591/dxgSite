<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shahenpc.system.mapper.oa.OaMeetingMapper">

    <resultMap type="OaMeeting" id="OaMeetingResult">
        <result property="meetingId"    column="meeting_id"    />
        <result property="title"    column="title"    />
        <result property="meetingType"    column="meeting_type"    />
        <result property="startTime"    column="start_time"    />
        <result property="endTime"    column="end_time"    />
        <result property="address"    column="address"    />
        <result property="content"    column="content"    />
        <result property="isSign"    column="is_sign"    />
        <result property="status"    column="status"    />
        <result property="remark"    column="remark"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="createBy"    column="create_by"    />
    </resultMap>

    <sql id="selectOaMeetingVo">
        select meeting_id, title, meeting_type, start_time, end_time, address, content, is_sign, status, remark, create_time, update_by, update_time, create_by from oa_meeting
    </sql>

    <select id="selectOaMeetingList" parameterType="OaMeeting" resultMap="OaMeetingResult">
        <include refid="selectOaMeetingVo"/>
        <where>
            <if test="title != null  and title != ''"> and title = #{title}</if>
            <if test="meetingType != null "> and meeting_type = #{meetingType}</if>
            <if test="startTime != null "> and start_time = #{startTime}</if>
            <if test="endTime != null "> and end_time = #{endTime}</if>
            <if test="address != null  and address != ''"> and address = #{address}</if>
            <if test="content != null  and content != ''"> and content = #{content}</if>
            <if test="isSign != null "> and is_sign = #{isSign}</if>
            <if test="status != null "> and status = #{status}</if>
        </where>
    </select>

    <select id="selectOaMeetingByMeetingId" parameterType="Long" resultMap="OaMeetingResult">
        <include refid="selectOaMeetingVo"/>
        where meeting_id = #{meetingId}
    </select>

    <insert id="insertOaMeeting" parameterType="OaMeeting" useGeneratedKeys="true" keyProperty="meetingId">
        insert into oa_meeting
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null">title,</if>
            <if test="meetingType != null">meeting_type,</if>
            <if test="startTime != null">start_time,</if>
            <if test="endTime != null">end_time,</if>
            <if test="address != null">address,</if>
            <if test="content != null">content,</if>
            <if test="isSign != null">is_sign,</if>
            <if test="status != null">status,</if>
            <if test="remark != null">remark,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="createBy != null">create_by,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null">#{title},</if>
            <if test="meetingType != null">#{meetingType},</if>
            <if test="startTime != null">#{startTime},</if>
            <if test="endTime != null">#{endTime},</if>
            <if test="address != null">#{address},</if>
            <if test="content != null">#{content},</if>
            <if test="isSign != null">#{isSign},</if>
            <if test="status != null">#{status},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="createBy != null">#{createBy},</if>
        </trim>
    </insert>

    <update id="updateOaMeeting" parameterType="OaMeeting">
        update oa_meeting
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null">title = #{title},</if>
            <if test="meetingType != null">meeting_type = #{meetingType},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="address != null">address = #{address},</if>
            <if test="content != null">content = #{content},</if>
            <if test="isSign != null">is_sign = #{isSign},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
        </trim>
        where meeting_id = #{meetingId}
    </update>

    <delete id="deleteOaMeetingByMeetingId" parameterType="Long">
        delete from oa_meeting where meeting_id = #{meetingId}
    </delete>

    <delete id="deleteOaMeetingByMeetingIds" parameterType="String">
        delete from oa_meeting where meeting_id in
        <foreach item="meetingId" collection="array" open="(" separator="," close=")">
            #{meetingId}
        </foreach>
    </delete>

    <!--=======================================================分割线-->
    <resultMap id="webDetail" type="com.shahenpc.system.domain.oa.dto.MeetingDetailDto" extends="OaMeetingResult">
        <association property="record" column="meeting_id" select="com.shahenpc.system.mapper.oa.OaMeetingRecordMapper.selectByMeetingId"/>
        <association property="sign" column="meeting_id" select="com.shahenpc.system.mapper.oa.OaMeetingSignMapper.selectByMeetingId"/>
        <association property="personnel" column="meeting_id" select="com.shahenpc.system.mapper.oa.OaMeetingPersonnelMapper.selectByMeetingId"/>
    </resultMap>
    <select id="newDetail" resultMap="webDetail" parameterType="Long" >
        <include refid="selectOaMeetingVo"/>
        where meeting_id = #{meetingId}
    </select>
</mapper>